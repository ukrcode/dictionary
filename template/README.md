# Шаблон файлу з перекладом

`exampl.yml` - це шаблон файлу, який використовується для перекладу текстів у вашому проекті. Цей файл містить ключі та відповідні їм значення, які використовуються для відображення тексту на різних мовах.